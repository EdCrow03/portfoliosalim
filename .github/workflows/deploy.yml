name: CI/CD Pipeline

on:
  push:
    branches:
      - main

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    steps:
      - name: 📥 Checkout repo
        uses: actions/checkout@v3

      - name: 🐳 Login to DockerHub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.dockerusername }}
          password: ${{ secrets.dockerpassword }}

      - name: 🔧 Build Docker image
        run: docker build -t ${{ secrets.dockerusername }}/portfolio:latest .

      - name: 📤 Push Docker image
        run: docker push ${{ secrets.dockerusername }}/portfolio:latest

  setup-kubeconfig:
    runs-on: ubuntu-latest
    needs: build-and-push
    steps:
      - name: 🔑 Configure kubeconfig
        run: |
          mkdir -p $HOME/.kube
          echo "${{ secrets.KUBE_CONFIG_DATA }}" > $HOME/.kube/config
          chmod 600 $HOME/.kube/config
        env:
          KUBECONFIG: $HOME/.kube/config

  deploy:
    runs-on: ubuntu-latest
    needs: setup-kubeconfig
    steps:
      - name: 📥 Checkout repo
        uses: actions/checkout@v3

      - name: ☸️ Deploy manifests with kubectl (validation disabled)
        run: |
          kubectl apply --validate=false -f k8s/deployment.yaml -n default
          kubectl apply --validate=false -f k8s/service.yaml -n default
        env:
          KUBECONFIG: $HOME/.kube/config

      - name: 🔍 Check pod status
        run: |
          kubectl get pods --namespace default
          kubectl describe pod -l app=portfolio --namespace default
        env:
          KUBECONFIG: $HOME/.kube/config

      - name: 📜 Fetch pod logs
        run: kubectl logs -l app=portfolio --namespace default
        env:
          KUBECONFIG: $HOME/.kube/config
